<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# guided_breathing Archive - guided_breathing_1767964023309

This directory is an archived generated copy inside the guided_breathing prototype. It is not the canonical working tree for this prototype. Use the top-level `guided_breathing/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Flutter export
- Intended product direction: guided_breathing
- Status: archive snapshot, not the canonical prototype root

## What This Means
- This export is a standalone Flutter app snapshot.
- It may preserve domain-themed UI or mobile experiments, but it is outside the active top-level scaffold workflow.
- Its README should be read as archive guidance, not as the primary product contract for guided_breathing.

## Setup
1. Install Flutter and Dart.
2. Run `flutter pub get`.
3. Run `flutter run`.

## Working Rule
Use the top-level `guided_breathing/README.md` to understand current product intent and maturity. Use this archive only for recovery, comparison, or mobile-specific extraction work.
