<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# Digital Agency Archive - atelier_studio_1768072148880

This directory is an archived generated copy inside the Digital Agency prototype. It is not the canonical working tree for this prototype. Use the top-level `Digital Agency/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Next.js export
- Intended product direction: Digital Agency
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Use this folder only if you intentionally want to revive or compare this standalone Next.js export. For current prototype guidance, use `Digital Agency/README.md`.
