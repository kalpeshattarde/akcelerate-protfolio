<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# REPLIT Advanced Prompt - event_registration

Run this prototype root as the active codebase for event_registration.

## Reality Check
- The code here is still the shared VelocityCore customer-operations scaffold.
- The directory name does not mean a complete event_registration exists yet.

## Setup
1. Run `npm install`.
2. Start Postgres and Redis or provide equivalents.
3. Copy `.env.example` to `.env`.
4. Run `npm run db:generate`.
5. Run `npm run db:push`.
6. Run `npm run db:seed`.
7. Run `npm run dev`.

## Development Goal
Use the running scaffold to identify and replace the generic customer-operations assumptions with real event_registration behavior.
