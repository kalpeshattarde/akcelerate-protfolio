<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# event_registration Archive - event_registration_1768024458403

This directory is an archived generated copy inside the event_registration prototype. It is not the canonical working tree for this prototype. Use the top-level `event_registration/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: unknown generated archive
- Intended product direction: event_registration

## Working Rule
Inspect the files in this directory before relying on them. The canonical prototype guidance lives in `event_registration/README.md`.
