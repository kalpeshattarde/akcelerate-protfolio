<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# Modern Restaurant Archive - pesto_restaurant_web_app_1767962403452

This directory is an archived generated copy inside the Modern Restaurant prototype. It is not the canonical working tree for this prototype. Use the top-level `Modern Restaurant/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Vite export
- Intended product direction: Modern Restaurant
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Treat this as a generated frontend snapshot. Use the top-level `Modern Restaurant/README.md` and manifest as the current source of truth.
