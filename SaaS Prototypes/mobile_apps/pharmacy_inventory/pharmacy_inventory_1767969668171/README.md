<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# pharmacy_inventory Archive - pharmacy_inventory_1767969668171

This directory is an archived generated copy inside the pharmacy_inventory prototype. It is not the canonical working tree for this prototype. Use the top-level `pharmacy_inventory/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Vite export
- Intended product direction: pharmacy_inventory
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Treat this as a generated frontend snapshot. Use the top-level `pharmacy_inventory/README.md` and manifest as the current source of truth.
