<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# Video Editing Archive - cineai_studio_1767968591761

This directory is an archived generated copy inside the Video Editing prototype. It is not the canonical working tree for this prototype. Use the top-level `Video Editing/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Flutter export
- Intended product direction: Video Editing
- Status: archive snapshot, not the canonical prototype root

## What This Means
- This export is a standalone Flutter app snapshot.
- It may preserve domain-themed UI or mobile experiments, but it is outside the active top-level scaffold workflow.
- Its README should be read as archive guidance, not as the primary product contract for Video Editing.

## Setup
1. Install Flutter and Dart.
2. Run `flutter pub get`.
3. Run `flutter run`.

## Working Rule
Use the top-level `Video Editing/README.md` to understand current product intent and maturity. Use this archive only for recovery, comparison, or mobile-specific extraction work.
