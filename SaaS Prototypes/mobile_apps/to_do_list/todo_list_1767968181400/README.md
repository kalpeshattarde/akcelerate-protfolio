<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# to_do_list Archive - todo_list_1767968181400

This directory is an archived generated copy inside the to_do_list prototype. It is not the canonical working tree for this prototype. Use the top-level `to_do_list/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Flutter export
- Intended product direction: to_do_list
- Status: archive snapshot, not the canonical prototype root

## What This Means
- This export is a standalone Flutter app snapshot.
- It may preserve domain-themed UI or mobile experiments, but it is outside the active top-level scaffold workflow.
- Its README should be read as archive guidance, not as the primary product contract for to_do_list.

## Setup
1. Install Flutter and Dart.
2. Run `flutter pub get`.
3. Run `flutter run`.

## Working Rule
Use the top-level `to_do_list/README.md` to understand current product intent and maturity. Use this archive only for recovery, comparison, or mobile-specific extraction work.
