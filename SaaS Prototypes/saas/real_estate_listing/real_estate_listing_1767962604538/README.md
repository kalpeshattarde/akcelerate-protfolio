<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# real_estate_listing Archive - real_estate_listing_1767962604538

This directory is an archived generated copy inside the real_estate_listing prototype. It is not the canonical working tree for this prototype. Use the top-level `real_estate_listing/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Vite export
- Intended product direction: real_estate_listing
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Treat this as a generated frontend snapshot. Use the top-level `real_estate_listing/README.md` and manifest as the current source of truth.
