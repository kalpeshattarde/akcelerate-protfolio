<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# marketing_analytics Archive - marketing_analytics_1768025911512

This directory is an archived generated copy inside the marketing_analytics prototype. It is not the canonical working tree for this prototype. Use the top-level `marketing_analytics/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Vite export
- Intended product direction: marketing_analytics
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Treat this as a generated frontend snapshot. Use the top-level `marketing_analytics/README.md` and manifest as the current source of truth.
