<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# E Learning Platform Archive - eduzen_academy_1768024714535

This directory is an archived generated copy inside the E Learning Platform prototype. It is not the canonical working tree for this prototype. Use the top-level `E Learning Platform/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Next.js export
- Intended product direction: E Learning Platform
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Use this folder only if you intentionally want to revive or compare this standalone Next.js export. For current prototype guidance, use `E Learning Platform/README.md`.
