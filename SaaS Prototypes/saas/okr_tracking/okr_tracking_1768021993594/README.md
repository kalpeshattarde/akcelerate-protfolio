<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# okr_tracking Archive - okr_tracking_1768021993594

This directory is an archived generated copy inside the okr_tracking prototype. It is not the canonical working tree for this prototype. Use the top-level `okr_tracking/` directory as the primary source of truth unless you are intentionally reviving this archive.

## Archive Type
- Type: generated Vite export
- Intended product direction: okr_tracking
- Status: archive snapshot, not the canonical prototype root

## Setup
1. Run `npm install`.
2. Copy any required env vars into a local `.env` file.
3. Run `npm run dev`.

## Working Rule
Treat this as a generated frontend snapshot. Use the top-level `okr_tracking/README.md` and manifest as the current source of truth.
