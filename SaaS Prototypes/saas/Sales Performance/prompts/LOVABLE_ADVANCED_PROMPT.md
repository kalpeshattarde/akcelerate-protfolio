<!-- Generated from prototype.manifest.json by `npm run prototypes:generate`. Edit the manifest and rerun the generator. -->
# LOVABLE Advanced Prompt - Sales Performance

Design inside this prototype root using the active code as truth.

## Truth First
- This directory is labeled Sales Performance, but the active code is still the shared VelocityCore customer-operations scaffold.
- Do not invent finished Sales Performance screens or workflows unless you also implement the supporting backend and data model.

## UX Direction
- Treat the current app as a reusable operator console scaffold.
- If the task is to convert it into a real Sales Performance, first design the domain model, core workflow, and onboarding path.
- Show clearly what is implemented, scaffolded, and planned.

## Avoid
- Generic template marketing pages.
- Fake domain dashboards that do not match stored data.
- Pretending auth, analytics, or automation are complete when they are placeholders.
